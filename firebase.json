{
  "database": {
    "rules": "database.rules.json"
  },
  "hosting": {
    "public": "build",
    "predeploy":["yarn install", "yarn build"],
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
